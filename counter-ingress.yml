apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: counter
spec:
  rules:
    - host: counter.192.168.64.26.nip.io
      http:
        paths:
          - path: /
            backend:
              serviceName: counter
              servicePort: 80
